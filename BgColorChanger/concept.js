
function red() {
    let color=document.querySelector('.Red');
    let stylee=getComputedStyle(color)
    document.body.style.backgroundColor =stylee.backgroundColor;
    console.log(stylee.backgroundColor) 
}
function blue() {
    let color=document.querySelector('.Blue');
    let stylee=getComputedStyle(color)
    document.body.style.backgroundColor =stylee.backgroundColor;
    console.log(stylee.backgroundColor) 
}
function green() {
    let color=document.querySelector('.Green');
    let stylee=getComputedStyle(color)
    document.body.style.backgroundColor =stylee.backgroundColor;
    console.log(stylee.backgroundColor) 
}
function yellow() {
    let color=document.querySelector('.Yellow');
    let stylee=getComputedStyle(color)
    document.body.style.backgroundColor =stylee.backgroundColor;
    console.log(stylee.backgroundColor) 
}
